name: Selenium Tests CI

on:
  push:
    branches: [ main]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        browser: [chrome, firefox]
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: Install Browsers
        run: |
          # Install Chrome
          wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list'
          
          # Install Firefox
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable firefox
          
      - name: Run tests
        run: |
          pytest tests/ \
            --alluredir=allure-results-${{ matrix.browser }} \
            --browser=${{ matrix.browser }} \
            --html=report-${{ matrix.browser }}.html \
            --self-contained-html \
            -v
        continue-on-error: true
        
      - name: Upload to Allure Docker Service
        if: always()
        env:
          ALLURE_URL: ${{ secrets.ALLURE_SERVER_URL }}  # New secret for Allure service
        run: |
          BROWSER="${{ matrix.browser }}"
          RESULTS_DIR="allure-results-${BROWSER}"
          
          if [ -d "$RESULTS_DIR" ]; then
            cd "$RESULTS_DIR"
            tar -czf ../allure-results-${BROWSER}.tar.gz .
            cd ..
            
            PROJECT_NAME="selenium-tests-${BROWSER}"
            BUILD_ID="${{ github.run_number }}"
            
            echo "Uploading ${BROWSER} results to Allure Docker Service..."
            RESPONSE=$(curl -X POST \
              -H "Content-Type: application/gzip" \
              --data-binary @allure-results-${BROWSER}.tar.gz \
              -L \
              -w "\nHTTP Status: %{http_code}\n" \
              -s \
              "${ALLURE_URL}/allure-docker-service/send-results?project_id=${PROJECT_NAME}")
            
            echo "$RESPONSE"
            
            HTTP_CODE=$(echo "$RESPONSE" | tail -n 1 | grep -oP '\d+')
            if [ "$HTTP_CODE" = "200" ]; then
              echo "✓ ${BROWSER} report uploaded successfully!"
              echo "View report at: ${ALLURE_URL}/allure-docker-service/projects/${PROJECT_NAME}/reports/latest/index.html"
            else
              echo "✗ Upload failed with status: $HTTP_CODE"
              exit 1
            fi
          else
            echo "No ${RESULTS_DIR} directory found"
          fi
          
      - name: Upload HTML Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-report-${{ matrix.browser }}-${{ github.run_number }}
          path: report-${{ matrix.browser }}.html
          retention-days: 30